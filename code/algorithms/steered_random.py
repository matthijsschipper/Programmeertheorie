from copy import deepcopy
from random import choice

def steered_random_routes(grid):
    """
    Algorithm that takes in a grid object as input
    First selects which routes to lay down first
    """